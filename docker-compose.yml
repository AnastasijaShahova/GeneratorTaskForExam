version: "3.3"

services:
  frontend:
    container_name: frontend
    build: ./client
    volumes:
      - './:/app'
      - '/app/node_modules'
    ports:
      - 3000:3000
    restart: always
    command: npm start
